"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.cloneClassInstance = cloneClassInstance;
exports.randomHex = randomHex;

var _util = require("util");

var _crypto = require("crypto");

const random = (0, _util.promisify)(_crypto.randomBytes);

function cloneClassInstance(object) {
  return Object.assign(Object.create(object), object);
}

async function randomHex(nBytes = 16) {
  const bytes = await random(nBytes);
  return bytes.toString('hex');
}